#include "main.h"
/**
  * _abs - The entry point of the function
  * @x: The argument to be evaluated
  *
  * Description: This is a function that computes the absolute value of an
  * integer.
  * Return: abs_val
  */
int _abs(int n)
{
	int abs_val;

	if (x < 0)
		abs_val = -1 * x;
	else
		abs_val = x;
	return (abs_val);
}
